#include <iostream>

using namespace std;

int main()
{   
    int a, b,c,d;
    cout << "Input 4 integers a, b, c and d" << endl;
    cin >> a >> b >> c >> d;
    
    cout << (a>b||c>d) <<endl;
    cout << (a>b && c>d) << endl;
    
    return 0;
}
